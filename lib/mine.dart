import 'package:flutter/material.dart';
import 'package:mynote/readCode.dart';

class MyMineWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Mine"),);
  }

}